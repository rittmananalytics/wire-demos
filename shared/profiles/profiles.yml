# dbt expects profiles.yml in --profiles-dir.
acme:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: ./warehouse.duckdb
      threads: 1
      extensions:
        - httpfs
        - parquet
